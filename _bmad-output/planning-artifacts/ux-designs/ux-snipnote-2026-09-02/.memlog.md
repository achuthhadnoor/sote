---
topic: snipnote UX - Full-Size Markdown Editor
updated: 2026-09-08T16:51
---

- (decision) Fast path: visual identity locked to localeditor.app — light minimal monochrome (white editor, #F8F8F9 sidebar, #EAEAEA hairlines, black primary, link blue #2563EB), single sans family, 760px centered, flat + one elevated palette
- (decision) IA locked: single full-size window (Sidebar 260px fixed: Search→File Tree→Library, Tab Bar 40px + Editor centered 760px + Status 24px, Command Palette Cmd+P, File Changed banner)
- (decision) Flows drafted: UJ-1 Alex side-by-side and UJ-2 Priya capture, both realizing PRD UJs, with failure branches for missing vault and dirty external change
- (event) HTML versions rendered: DESIGN.html and EXPERIENCE.html self-contained inline CSS, LocalEditor theme, 760px centered mock
- (decision) Vibrant window: macOS Sidebar + Windows Mica via EffectsBuilder only (tauri macos-private-api, transparent:true, html transparent, translucent surfaces rgba 0.68-0.78, opaque fallback, palette stays opaque, radius 12)
- (override) Window chrome: Overlay + empty title macOS-only; Windows/Linux native decorations + titled window; TabBar is content chrome only on Windows (caption buttons in system title bar)
- (override) EffectsBuilder split by cfg: Effect::Sidebar+radius12 on macOS; Effect::Mica only on Windows 11 (soft-fail older); no combined [Sidebar,Mica] list
- (decision) Platform scope: macOS + Windows shipping (CI windows-latest + release artifacts); Linux opaque fallback remains non-QA
- (decision) Shortcut labels via modShortcut/platform.ts: ⌘ on mac, Ctrl+ on Windows/Linux; accelerators stay CmdOrCtrl
- (decision) WelcomeGate: brand-first drop zone + Choose Folder + drag-drop vault open; replaces plain No vault open empty state
- (decision) Settings is an editor tab (SettingsView) not modal overlay: Appearance (theme + tint hue/amount), Spell check, Autostart, Check for Updates, Logs
- (decision) Fast-path Update 2026-09-08: sync DESIGN.md + EXPERIENCE.md spines to shipped Windows-compatible release state
- (decision) Sidebar collapsible via CmdOrCtrl+B / TabBar toggle; welcome mode hides sidebar entirely
- (decision) Accent is user-tintable hue/intensity (CSS --theme-tint-hue/amount); Reduce Transparency forces opaque fills; link blue no longer the only chromatic token
- (decision) Find/Replace in editor (FindBar) and system spellcheck toggle shipped; HomeView search within vault when no note open
- (event) spines finalized 2026-09-08 fast-path Update: DESIGN.md + EXPERIENCE.md synced to shipped Windows/macOS release state; status final
- (event) UX spines flipped to floating-first v1; full shell documented as v2
