/** @deprecated Import from `src/utils/paths` — re-export bridge for comparison helpers. */
export {
  canonicalPath,
  isPathWithin,
  joinPath,
  normalizePath,
  pathBasename,
  pathJoin,
  nextUntitledNotePath,
} from "../utils/paths";
