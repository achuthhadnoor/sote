---
idea: fastest markdown editor/viewer along with metadata using tauri v2
goal: TBD
status: complete
updated: 2026-09-02T08:42
---

- (direction) Idea refined: Markdown as a human-readable database for AI agents. Build using Tiptap editor with todo lists, mermaid charts, and inline images.
- (direction) User proposes switching from Tiptap to Monaco Editor to resolve the tension between speed, strict AI-friendly Markdown, and rich features.
- (note) Cursor achieves inline UI in Monaco Editor by heavily customizing Monaco's ZoneWidget and ContentWidget APIs to inject React/Web components directly into the text flow.
- (direction) User redefines scope: 'A lightweight, local editor for the Markdown, HTML, YAML, JSON & config files your agents write.'
- (lock) User locks in Tiptap as the primary editor and adds an Excalidraw-like canvas feature, prioritizing rich WYSIWYG experience over pure code-editor speed.
- (crack) Tiptap does not natively support syntax highlighting, code folding, or schema validation for YAML, JSON, and config files, requiring complex custom extensions to handle these formats gracefully.
- (note) User asks about Raycast Notes. Raycast is built natively (Swift/AppKit), so its editor likely uses native NSTextView/TextKit rather than a web-based editor, allowing extreme speed but requiring platform-specific development.
- (direction) User considers pivoting from Tauri/Web stack to GPUI (Rust GPU-accelerated UI framework) to achieve maximum speed.
- (crack) User asks if they can embed a webview inside GPUI to run web technologies. This defeats the purpose of GPUI and GPUI does not currently have robust webview support. It's just reinventing Tauri but with more pain.
- (lock) User locks in Tauri v2, Tiptap with rich blocks (Mermaid), Excalidraw for canvas. Source of truth is raw markdown for AI agents; app parses to/from rich state.
- (direction) User solves the data storage issue: Canvas data (JSON) and Markdown data are saved in separate files. The UI allows toggling between Note mode and Canvas mode. Added 'floating notes' feature for quick access.
- (direction) User expands vision: The app is a unified workspace for human-agent collaboration containing Note (Knowledge base), Canvas (App flows), and Terminal (Agent execution) tabs.
- (lock) User re-targets to Claude Code terminal lovers, positioning vs Cursor 3.0 (large IDE). Differentiated from Mote (https://getmote.md) — Mote is floating native Swift <150MB, macOS-only, $19 with terminal + Claude integration. User positions as full-size cross-platform companion, not floating.
- (crack) Mote validates market but proves native beats Tauri on speed; full-size market more crowded than floating (Obsidian, Typora). Generic simple editor has no wedge.
- (lock) User locks Build-in-Public incremental strategy: v1 = full-size simple Markdown note editor (Tauri v2 + Tiptap, file-watcher, raw Markdown DB), then sequentially add Obsidian vault -> Canvas (Excalidraw separate files) -> Embedded Terminal (xterm + PTY) -> Floating notes. Rejects big-bang workspace for first dollar. Attack mode ended.
