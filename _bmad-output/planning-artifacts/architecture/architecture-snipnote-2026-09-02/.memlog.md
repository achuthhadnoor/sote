---
scope: snipnote desktop markdown editor
purpose: build-substrate
altitude: feature
updated: 2026-09-08T16:51
---

- (event) started architecture spine creation for snipnote
- (constraint) Input PRD: _bmad-output/planning-artifacts/prds/prd-snipnote-2026-09-02/prd.md
- (constraint) Input UX: _bmad-output/planning-artifacts/ux-designs/ux-snipnote-2026-09-02/DESIGN.md
- (constraint) Existing repo stack: Tauri v2, React 19, TypeScript ~5.8, Vite 7
- (direction) User selected Coaching path
- (decision) AD-1 [ADOPTED]: Rust backend owns all disk I/O, atomic writes (temp file + rename), directory indexing, and file watching via notify. Prevents partial/corrupt writes on crash, echo loops, and missing OS-level file events. Binds Tauri IPC and storage layer.
- (decision) AD-2 [ADOPTED]: Note Document uses Envelope Model (rawFrontmatter string + body string). Frontmatter is sliced verbatim on read and prepended verbatim on write, completely shielded from Tiptap/ProseMirror DOM manipulation to guarantee byte-exact metadata preservation.
- (decision) AD-3 [ADOPTED]: Rust file-watcher implements echo suppression using a thread-safe RecentlyWritten cache (path + mtime/hash with TTL). Prevents self-inflicted reload/banner loops on user saves.
- (decision) AD-4 [ADOPTED]: External change handling rule: if modified file is clean (dirty=false), auto-reload and preserve scroll/cursor; if modified file has unsaved edits (dirty=true), display non-blocking banner 'File changed on disk — [Reload] [Keep mine]' without clobbering user buffer.
- (decision) AD-5 [ADOPTED]: Frontend state managed via isolated Zustand stores (vaultStore, tabStore, editorStore). Keystroke document statistics (words/chars/paragraphs) use isolated subscriptions to prevent re-rendering sidebar/tabs. Code blocks use custom React NodeView toggling between raw markdown text and Mermaid SVG rendering.
- (decision) AD-6 [ADOPTED]: App session and window geometry persisted via Rust-managed session.json in APP_CONFIG_DIR. Loaded in Rust setup lifecycle to eliminate window geometry flicker and restore vault attachment before WebView render.
- (decision) AD-7 [ADOPTED]: Write debouncing and save policy: Keystrokes debounce-save at 500ms inactivity, with immediate flush on tab-switch, blur, and window close. Guarantees disk matches buffer within 500ms without flooding I/O.
- (decision) AD-8 [ADOPTED]: Note creation semantics: Clicking '+' immediately writes 'Untitled.md' (or incremented counter) to disk in active folder/vault root and emits vault tree update. Preserves strict 1:1 disk mirror invariant so external agents see new files immediately.
- (event) spine finalized
- (event) Correct Course 2026-09-08: update AD-9/10/11 + seed + FR map for Windows chrome split, SettingsView, updater
- (event) Architecture spine finalized 2026-09-08: AD-9/10/11 updated, AD-12 updater, seed + FR map synced
- (event) AD-13 dual presentation adopted
